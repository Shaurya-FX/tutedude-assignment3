# tutedude-assignment3  


assignment3-task1 :

A simple Python program that calculates the factorial of a user-entered non-negative integer using a function and a loop, runs on Python 3, takes input via the terminal, and prints the result (e.g., input 5 → output 120), making it suitable for beginners learning functions, loops, and user input.


assignment3-task2 :

A simple Python program that uses the built-in `math` module to calculate and display the square root, natural logarithm, and sine of a user-entered number, helping beginners practice user input, mathematical functions, and module usage in Python.
